name: ImportVerifier release check

on:
  pull_request:
  push:
    branches:
      - feat/import-rules-verifier-branding

concurrency:
  group: importverifier-release-${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Diagnostic tests A-F
        run: node --import tsx --test tests/[a-f]*.test.ts
      - name: Diagnostic tests G-I
        run: node --import tsx --test tests/[g-i]*.test.ts
      - name: Diagnostic tests J-L
        run: node --import tsx --test tests/[j-l]*.test.ts
      - name: Diagnostic tests M-R
        run: node --import tsx --test tests/[m-r]*.test.ts
      - name: Diagnostic tests S-Z
        run: node --import tsx --test tests/[s-z]*.test.ts
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
        env:
          # CI uses a neutral HTTPS origin. Production injects the canonical Sites URL.
          NEXT_PUBLIC_SITE_URL: https://ci.importverifier.example
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
